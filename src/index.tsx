import * as React from 'react';
import * as ReactDOM from 'react-dom';

const title = 'My Minimal React Webpack TypeScript Setup';

ReactDOM.render(<div>{title}</div>, document.getElementById('app'));
